---
interface Props {
  src: ImageMetadata;
  alt: string;
  caption: string;
  kind: "diagram";
  // Keep authored SVG support while this branch migrates existing lesson figures.
  // The learning-site validator enforces the stricter lesson policy.
  provenance: "authored-svg" | "ai-generated-raster";
  claimStatus: "conceptual" | "not_evidence";
}

const { src, alt, caption, kind, provenance, claimStatus } = Astro.props;
---

<figure
  class="lesson-figure"
  data-figure-kind={kind}
  data-provenance={provenance}
  data-claim-status={claimStatus}
>
  <img src={src.src} width={src.width} height={src.height} alt={alt} loading="lazy" decoding="async" />
  <figcaption>{caption}</figcaption>
</figure>
