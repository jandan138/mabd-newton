---
title: "刚体动力学基础"
description: "用自由度、速度、惯量和动量理解刚体模拟为何是 ABD 的重要参照。"
module: "Physics"
order: 6
status: "complete"
claimStatus: "conceptual"
repoEvidence: []
---

import ConceptCard from "../../components/ConceptCard.astro";
import ProblemCard from "../../components/ProblemCard.astro";
import StepFlow from "../../components/StepFlow.astro";
import CompareGrid from "../../components/CompareGrid.astro";
import Figure from "../../components/Figure.astro";
import PseudoCode from "../../components/PseudoCode.astro";
import PitfallCard from "../../components/PitfallCard.astro";
import RememberBox from "../../components/RememberBox.astro";
import RepoEvidenceCard from "../../components/RepoEvidenceCard.astro";
import LearningGoals from "../../components/LearningGoals.astro";
import PrereqBox from "../../components/PrereqBox.astro";
import GuidedProjectStep from "../../components/GuidedProjectStep.astro";
import CheckpointQuiz from "../../components/CheckpointQuiz.astro";
import PracticePrompt from "../../components/PracticePrompt.astro";
import WorkedExercise from "../../components/WorkedExercise.astro";
import rigidVsAffineDof from "../../assets/diagrams/rigid-vs-affine-dof.png";

<ProblemCard title="它解决的问题">
刚体是理解 ABD 的重要参照：它只有平移和旋转，没有拉伸和剪切。先理解 6 DOF、角速度和惯量，再看 12 维仿射状态，会更清楚 ABD 多出来的自由度在表达什么。
</ProblemCard>

<Figure
  src={rigidVsAffineDof}
  alt="Rigid body with move and rotate arrows compared to affine body with additional stretch and shear arrows"
  caption="Conceptual DOF comparison: rigid bodies use 6 DOF, while affine state uses 12 numbers for `A` and `t`."
  kind="diagram"
  provenance="ai-generated-raster"
  claimStatus="conceptual"
/>

<LearningGoals>
  <li><strong>解释自由度：</strong>说明三维刚体为什么是 6 DOF。</li>
  <li><strong>区分速度：</strong>把质心位置速度和角速度分开理解。</li>
  <li><strong>建立参照：</strong>说出刚体模型如何帮助理解 ABD 多出的自由度。</li>
</LearningGoals>

<PrereqBox>
  <li>知道位置和速度分别描述“在哪里”和“怎样变化”。</li>
  <li>见过旋转的基本概念，能接受朝向不是普通位置坐标。</li>
  <li>已经读过 `affine-state` 会更容易对比刚体与仿射体。</li>
</PrereqBox>

<GuidedProjectStep step="贯穿小项目 6/12" title="让印章先通过刚体版本">
把同一枚软橡胶印章暂时换成 rigid-only 版本：它可以平移、转动，但标记点之间的距离和角度永远不能变。这样能看清 6 DOF 刚体能表达整体运动，却不能表达印章被压扁、拉长或剪切。
</GuidedProjectStep>

## 6 DOF：三个位置，加三个姿态

三维刚体的形状不变。它需要 3 个平移自由度描述质心在哪里，还需要 3 个旋转自由度描述朝向。虽然代码里常用旋转矩阵或四元数存姿态，但真实独立自由度仍是 3 个旋转方向。

<ConceptCard title="刚体像一把不会弯的尺子">
无论你怎么移动或转动尺子，尺子上任意两点的距离都不变。模拟器要做的是更新整把尺子的位置和朝向，而不是让尺子局部伸长。
</ConceptCard>

## 速度、动量和惯量

平移速度描述质心怎么走；角速度描述物体绕哪个轴、以多快速度转。线动量通常与质量和线速度相关，角动量则与惯性张量和角速度相关。

惯性张量可以类比为“旋转版的质量”。同样的推力矩，细长杆绕长轴转和绕短轴转的难易不同，因为质量分布相对旋转轴不同。

<CompareGrid title="旋转表示对比">
  <article><h4>旋转矩阵</h4><p>3x3，几何直观，列向量就是刚体局部坐标轴，但需要保持正交约束。</p></article>
  <article><h4>四元数</h4><p>4 个数字，常用于稳定地存储和插值旋转，但需要单位长度约束。</p></article>
  <article><h4>仿射矩阵</h4><p>3x3，可表达旋转，也可表达拉伸和剪切；若只表示刚体旋转，就必须限制为纯旋转。</p></article>
</CompareGrid>

<PseudoCode title="刚体更新的概念伪代码">
{`x = x + dt * v
R = integrate_rotation(R, omega, dt)

linear_momentum = mass * v
angular_momentum = inertia_world @ omega`}
</PseudoCode>

<StepFlow title="为什么旋转让模拟变非线性">
  <li>位置可以直接相加，旋转不能随便把矩阵元素线性相加后就当作合法旋转。</li>
  <li>旋转矩阵必须保持列向量正交且长度为 1。</li>
  <li>惯性张量会随姿态从局部坐标变到世界坐标。</li>
  <li>角速度和姿态更新之间存在乘法关系，因此方程自然带有非线性。</li>
</StepFlow>

<PitfallCard title="存储维度不等于自由度">
四元数用 4 个数字，旋转矩阵用 9 个数字，但三维旋转本身只有 3 个自由度。额外数字通常带有约束，例如单位四元数或正交矩阵。
</PitfallCard>

<CheckpointQuiz>
  <details>
    <summary>为什么 3D 刚体的旋转不是 9 DOF？</summary>
    <p>旋转矩阵虽然有 9 个数字，但它的列向量必须保持单位长度且彼此正交，这些约束把独立旋转自由度限制为 3 个。</p>
  </details>
  <details>
    <summary>ABD 相比刚体多表达了什么？</summary>
    <p>刚体只表达平移和旋转；仿射体的 `A` 还可以表达拉伸、缩放和剪切，因此能用少量自由度描述更丰富的整体形变。</p>
  </details>
</CheckpointQuiz>

<RepoEvidenceCard title="本页只提供物理参照" status="本课为 conceptual" />

<PracticePrompt title="动手练习">
列出一个刚体状态至少需要存的变量，再列出一个仿射体状态 `q = [A, t]` 需要存的变量，对比哪些变量负责位置、哪些变量负责形状或朝向。
</PracticePrompt>

<WorkedExercise title="带答案的练习：刚体状态和 q=[A,t]">
对同一枚印章，写出 rigid-only 状态和仿射状态 `q = [A, t]` 各自需要表达什么。请特别区分“独立运动自由度”和“程序里可能用多少个数字存储”。

<details>
  <summary>提示</summary>
  <p>刚体的独立运动是 3 个平移加 3 个旋转；旋转可以用四元数或旋转矩阵存，但这些存储有单位长度或正交约束。仿射状态则直接让 `A` 的 9 个数字表达更多线性变化。</p>
</details>

<details>
  <summary>参考答案</summary>
  <p>rigid-only 印章需要质心或参考点位置 `x` 加整体朝向 `R`，独立运动自由度是 6 个：3 个平移和 3 个旋转。即使代码用 4 个四元数数字或 9 个旋转矩阵数字存朝向，合法旋转仍只有 3 个独立自由度。仿射状态 `q = [A, t]` 存 `A` 的 9 个数字和 `t` 的 3 个数字；`t` 负责整体位置，`A` 可以包含旋转，也可以包含拉伸、缩放和剪切，所以它能表达刚体 6 DOF 不能表达的印章压扁或斜切。</p>
</details>
</WorkedExercise>

<RememberBox title="核心记忆">
  <ul>
    <li>刚体有 6 DOF：3 个平移加 3 个旋转。</li>
    <li>角速度、惯性张量和角动量描述旋转运动。</li>
    <li>旋转有约束和非线性，这也是 ABD 需要小心处理旋转部分的原因。</li>
  </ul>
</RememberBox>
